//---------------------------------------------------------------------------

#ifndef FieldH
#define FieldH
#include <vcl.h>
#include "Points.h"

void DrawField(TImage* image, int horizontalNumber, int verticalNumber);
void DrawPath(TImage *image, Points *point);
void DrawLine(TImage *image, Points *start, Points *finish, TColor color);
void PaintField(TImage* image, Points** points);
void ChangeFieldColor (TImage* image, Points* point);

//---------------------------------------------------------------------------
#endif
